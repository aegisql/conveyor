spring:
  config:
    activate:
      on-profile: demo
  security:
    oauth2:
      resourceserver:
        jwt:
          enabled: false

logging:
  level:
    com.aegisql.conveyor: DEBUG
    org.springframework.security: INFO
    org.springframework.web.client: INFO
